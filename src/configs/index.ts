/**
 * 支持 gridIndex 的组件
 */
export const supportGridIndexComps = ['xAxis', 'yAxis']

/**
 * 支持 xAxisIndex 和 yAxisIndex 的系列类型
 */
export const supportCartesianAxisIndexSeries = [
  'line',
  'bar',
  'scatter',
  'effectScatter',
  'boxplot',
  'candlestick',
  'heatmap',
  'lines',
  'graph',
  'pictorialBar',
  'custom',
]
